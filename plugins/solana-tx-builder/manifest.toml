name = "solana-tx-builder"
version = "0.1.0"
description = "Local, pure-compute Solana instruction & address construction for the ZeroClaw agent: PDAs, ATAs, SystemProgram/SPL transfers. Zero network surface — the agent builds, a wallet signs."
author = "furkanefecancaglar"
wasm_path = "solana_tx_builder.wasm"
capabilities = ["tool"]
permissions = []
